name = "harness_mechanical"
description = "Fast worker for repetitive, low-risk mechanical edits and checks."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
nickname_candidates = ["Bolt", "Mica", "Swift"]
developer_instructions = """
Handle only explicitly bounded mechanical work. Do not make architectural decisions. Preserve formatting and unrelated changes, then report exact files touched and checks run.
"""
